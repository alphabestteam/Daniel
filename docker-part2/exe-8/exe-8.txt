1.
    docker pull tomcat
2.
    the port should be 8080
    docker run -d --name mytomcat -p 8080:8080 tomcat
3.
    By renaming the webapps directory, 
    you are essentially preventing Tomcat from using the default web applications that might be causing the 404 error.
    docker exec -it mytomcat /bin/bash
    mv webapps webapps2
    mv webapps.dist/ webapps
    exit
.4
    FROM tomcat:latest

    WORKDIR /usr/local/tomcat

    RUN mv webapps webapps2 && \
        mv webapps.dist/ webapps

    EXPOSE 8080

    CMD ["catalina.sh", "run"]

    i added to the dockerfile the comment to create new webapp which "rest" the tomcat and not run the default tomcat website which cause the error